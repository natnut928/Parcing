from bs4 import BeautifulSoup as bs
from pprint import pprint
from pymongo import MongoClient
import pymongo
import json
import re
import requests

# Подключение к MongoDB
re.client = MongoClient('localhost', 27017)

# Получить соединение с базой данных

re.mongodb = re.client['Vacancy_db']

# Получить подключение к коллекции

re.vacancy_id = re.mongodb['vacancy_id']

# re.vacancy_id.insert_one({'name': 'Ye Huan', 'age': 18, 'sex': 'Male'})

re.vacancy_id.insert_many([
    {'vacancy_name': 'Python Developer', 'company_name': 'OOO APPCraft', 'city': 'Moscow', 'salary': ''},
    {'vacancy_name': 'Python Web Developer', 'company_name': 'Soshace', 'city': 'N.Novgorod', 'salary': 80000},
    {'vacancy_name': 'Senior Python Developer', 'company_name': 'OOO Krypton', 'city': 'Moscow', 'salary': 150000},
    {'vacancy_name': 'Junior IT specialist', 'company_name': 'BR Lab', 'city': 'S.Petersburg', 'salary': 90000},
    {'vacancy_name': 'Python Backend Developer', 'company_name': 'Web Interactive World', 'city': 'Voronej','salary': 100000},
    {'vacancy_name': 'Junior Python Developer', 'company_name': 'MERA', 'city': 'Novosibirsk', 'salary': 95000}
])
